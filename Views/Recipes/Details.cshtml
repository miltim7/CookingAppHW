@model Recipe

@{
    ViewData["Title"] = "Details";
}

<p class="details-title">@Model.Title</p>

<div class="slider" x-data="{start: true, end: false}" style="padding-top: 150px;">
    <div class="slider__content" x-ref="slider"
        x-on:scroll.debounce="$refs.slider.scrollLeft == 0 ? start = true : start = false; Math.abs(($refs.slider.scrollWidth - $refs.slider.offsetWidth) - $refs.slider.scrollLeft) < 5 ? end = true : end = false;">
        <div class="slider__item"><img class="slider__image" src="https://source.unsplash.com/featured/?@Model.Title">
        </div>
        <div class="slider__item"><img class="slider__image"
                src="https://source.unsplash.com/featured/?my @Model.Title">
        </div>
        <div class="slider__item"><img class="slider__image"
                src="https://source.unsplash.com/featured/?big @Model.Title">
        </div>
        <div class="slider__item"><img class="slider__image"
                src="https://source.unsplash.com/featured/?delicious @Model.Title">
        </div>
        <div class="slider__item"><img class="slider__image"
                src="https://source.unsplash.com/featured/?tasty @Model.Title">
        </div>
        <div class="slider__item"><img class="slider__image"
                src="https://source.unsplash.com/featured/?large    @Model.Title">
        </div>
    </div>
</div>

<div class="details">
    <div style="background-color: #75ff93;">
        <label><b>Description</b></label>
        <p>@Model.Description</p>
    </div>

    <div>
        <label><b>Category</b></label>
        <p>@Model.Category</p>
    </div>

    <div>
        <label><b>Creator</b></label>
        <p>@ViewData["UserName"]</p>
    </div>

    <div>
        <label><b>Recipes id</b></label>
        <p>@Model.Id</p>
    </div>
</div>

<div style="top: 950px; position: absolute; height: 10px">ㅤ</div>

<style>
    .details {
        position: absolute;
        top: 700px;
        font-size: 20px;
        font-weight: 400;
        color: #000000;
        margin-right: 35px;

        display: flex;
        flex-direction: row;
        justify-content: space-between;
        gap: 20px;
        margin-bottom: 100px;
    }

    .details div {
        display: flex;
        flex-direction: column;
        background-color: #ededed;
        border-radius: 20px;
        padding: 50px;
        text-align: center;
        width: 265px;
        opacity: 0.8;
    }

    .details p {
        margin-top: 20px;
        margin-bottom: 50px;
    }

    .details-title {
        position: absolute;
        top: 150px;
        font-size: 40px;
        font-weight: bold;
        color: #97cd33;
    }

    :root {
        --scrollcolor: #fff;
        --scrollbackground: #141e27;
    }

    .details-ul {
        list-style: none;
        margin-top: 30px;
    }

    .title {
        font-size: 2.5rem;
        font-family: system-ui;
        line-height: 1.1;
        font-weight: 300;
        color: #fff;
        margin: 4rem auto 1rem;
        width: 85%;
        max-width: 1280px;
    }

    .slider {
        width: 85%;
        max-width: 1280px;
        margin: 100px auto 0 auto;
        display: flex;
        flex-direction: column;
    }

    .slider__content {
        overflow-x: scroll;
        scroll-snap-type: x mandatory;
        display: flex;
        gap: 1rem;
        padding-bottom: 1rem;
        scrollbar-color: var(--scrollcolor) var(--scrollbackground);
    }

    .slider__content::-webkit-scrollbar {
        height: 0.5rem;
        width: 0.5rem;
        border-radius: 1rem;
        background: var(--scrollbackground);
    }

    .slider__content::-webkit-scrollbar-thumb {
        border-radius: 1rem;
        background: var(--scrollcolor);
    }

    .slider__content::-webkit-scrollbar-track {
        border-radius: 1rem;
        background: var(--scrollbackground);
    }

    .slider__item {
        scroll-snap-align: start;
        display: flex;
        flex-direction: column;
        justify-content: flex-end;
        min-width: 350px;
        width: 350px;
        border-radius: 0.25rem;
        overflow: hidden;
        position: relative;
        aspect-ratio: 1;
    }

    .slider__image {
        display: block;
        width: 350px;
        height: 350px;
        object-fit: cover;
        position: absolute;
        top: 0;
        left: 0;
    }

    .slider__nav {
        margin: 1rem 0 4rem;
        width: 100%;
        padding: 0;
        display: flex;
        justify-content: flex-start;
        gap: 1rem;
        align-content: center;
        align-items: center;
        pointer-events: none;
    }
</style>