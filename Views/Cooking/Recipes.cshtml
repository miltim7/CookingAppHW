@model IEnumerable<Recipe>

@{
    ViewData["Title"] = "Recipes";
}

<ul>
    @foreach (var recipe in Model)
    {
        <div class="title">
            <li>@recipe.Title</li>
        </div>
        <div class="price">
            <li>@recipe.Price$</li>
        </div>
    }
</ul>